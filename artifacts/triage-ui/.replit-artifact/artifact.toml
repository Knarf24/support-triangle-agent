kind = "web"
previewPath = "/"
title = "Support Triage Agent"
version = "1.0.0"
id = "artifacts/triage-ui"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19809

[services.development]
run = "pnpm --filter @workspace/triage-ui run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/triage-ui", "run", "build" ]
publicDir = "artifacts/triage-ui/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19809"
BASE_PATH = "/"
